  Materialize.toast('<%=flash[:notice]%>', 4000);
  Materialize.toast('<%=flash[:danger]%>', 4000);
  var $collapsible =   $("[data-list-pending-invitation-id= '<%= @invitation.id %>']").parent();
  $collapsible .collapsible('close', 0)
  collapsibleClose($collapsible);
