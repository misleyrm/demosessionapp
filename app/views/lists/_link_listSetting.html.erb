  <!-- <%= link_to (user.owner?(list))? edit_list_path(list) : list_showList_path(list), data: { behavior: 'modal' },remote: true do%>
     <i class="material-icons md-15 md-light">settings</i>
  <% end %> -->
  <% if policy(list).update? %>
    <%= link_to edit_list_path(list), data: { behavior: 'modal' },remote: true do%>
       <i class="material-icons md-15 md-light">settings</i>
    <% end %>
  <% end %>
