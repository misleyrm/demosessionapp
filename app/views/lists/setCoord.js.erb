$('#popupcrop').css('display', 'none');
$('#popupcrop').css('opacity', 1);
<% if @list.errors.count==0 %>
   $('#form_updateAvatar').prepend("<%= j render :partial => 'shared/flash_messages', :locals => {:object => @list} %>");
<% else %>
   $('#form_updateAvatar').prepend("<%= j render :partial => 'shared/error_messages', :locals => {:object => @list} %>");
<% end %>
