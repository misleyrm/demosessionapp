<%= render :layout => 'notifications/li', :locals => {:notification => notification} do %>
     <%= "#{notification.actor.name} has cancelled the task: #{notification.notifiable.detail}" if  !notification.notifiable.blank?%>
<% end %>
