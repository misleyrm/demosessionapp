<div class="chip">
	<% if !user.image.file.nil? %>  
		<%= image_tag(user.image_url(:thumb), class: 'circle', size: "50x50") %>
	<% else %>
		<span><%= user.first_name[0]%></span>
	<% end%>
	<%= user.first_name %>
</div>
