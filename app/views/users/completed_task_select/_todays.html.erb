<% if user.completed_tasks_by_date(list,current_date).count!=0 %>
    <%= render partial: "users/completed_task", layout: "shared/collapsible_completed_task", locals: {  :date => current_date, :i => 0, :user => user, :list => list, :current_date => current_date} %>
<% end %>
