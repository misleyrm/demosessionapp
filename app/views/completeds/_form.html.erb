<%= form_for completed, remote: true do |f| %>
  <% if completed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(completed.errors.count, "error") %> prohibited this completed from being saved:</h2>

      <ul>
      <% completed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= image_tag "completed.png", :height => "15" %>
    <%= f.label :completed %><br>
    <%= f.text_area :completed %>
  </div>
  <!-- <div class="">
    <%#= f.submit %>
  </div> -->
<% end %>
