<p id="notice"><%= notice %></p>

<h1>Listing Completeds</h1>

<table>
  <thead>
    <tr>
      <th>Session</th>
      <th>User</th>
      <th>Completed</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @completeds.each do |completed| %>
      <tr>
        <td><%= completed.session_id %></td>
        <td><%= completed.user_id %></td>
        <td><%= completed.completed %></td>
        <td><%= link_to 'Show', completeds_path %></td>
        <td><%= link_to 'Edit', edit_completed_path(completed) %></td>
        <td><%= link_to 'Destroy', completed, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Completed', new_completed_path %>
